[project]
name = "eeg-feature-atlas"
version = "0.1.0"
description = "Reference examples for EEG feature analysis and visualization."
requires-python = ">=3.12,<3.13"
dependencies = [
    "edfio",
    "marimo",
    "matplotlib",
    "mne",
    "numpy",
    "nbformat", 
    "nbconvert",
    "playwright",
]